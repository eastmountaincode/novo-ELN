# Copy to .env.production before running the production Docker compose file.
# Do not reuse these defaults for a shared or public deployment.

ELN_SESSION_SECRET=replace-me
ELN_BOOTSTRAP_EMAIL=andrew@example.local
ELN_BOOTSTRAP_PASSWORD=Development-only-password-2026!

# Optional ENEX import tuning.
# ENEX_IMPORT_WORKERS=4
